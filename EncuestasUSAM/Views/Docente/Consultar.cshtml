@model List<EncuestasUSAM.Models.DOCENTECrud>

@{
    ViewBag.Title = "Docentes";
    Layout = "~/Views/Shared/_Principal.cshtml";
}

<div class="container">
    <h2>Docentes</h2>
    <div class="row">
        <div class="col-md-10">
            <table class="table table-hover table-striped">
                <thead style="background-color: #094f8b; color: #fff; font-size: 15px;">
                    <tr>
                        <th rowspan="2">Docente</th>
                        <th colspan="2">Correos</th>
                        <th rowspan="2">Facultad</th>
                        <th rowspan="2">Profesion</th>
                        <th rowspan="2">Deshabilitar / Habilitar</th>
                    </tr>
                    <tr>
                        <th>USAM</th>
                        <th>Personal</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var docente in Model)
                    {
                        <tr align="center">
                            <td>@docente.DOCENTE</td>
                            <td>@docente.CORREO_INSTITUCIONAL</td>
                            <td>@docente.CORREO_PERSONAL</td>
                            <td>@docente.NOMBRE_FACULTAD</td>
                            <td>@docente.NOMBRE_PROFESION</td>
                            <td>@docente.ESTADO_PERMISO</td>
                            <td>
                                <label class="switch">
                                    <input type="checkbox" value="@docente.ESTADO_PERMISO">
                                    <span class="slider round"></span>
                                </label>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>